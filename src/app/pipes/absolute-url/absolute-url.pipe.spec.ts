import { AbsoluteUrlPipe } from './absolute-url.pipe';

describe('AbsoluteUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsoluteUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
