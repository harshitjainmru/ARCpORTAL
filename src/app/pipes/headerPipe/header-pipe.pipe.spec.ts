import { HeaderPipePipe } from './header-pipe.pipe';

describe('HeaderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
